#include<stdio.h>
#include<math.h>
void main()
{
int array[100]={1,12,15,26,38};
int i,n;
printf("enter the number");
scanf("%d",&n);
for(i=0;i<=n;i++)
{
if(array[i]%2==0))
{
i=(array[i]+1)/2)
}
}
printf("median element");
}
