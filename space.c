#include<stdio.h>
#include<string.h>
void main()
{
char s[i];
int i,n=100,count=0;
printf("enter the string");
scanf("%s",&s[i]);
strlen(n);
for(i=0;i<n;i++)
{
if(s[i]=='\0')
{
count=count++;
}
printf("space=%d",count);
}
}
